- defaults:
    link:
      relink: true
      force: false

- clean: ['~']

- link:
    ~/.Xdefaults:
    ~/.alias:
    ~/.antigen.zsh:
    ~/.bash_logout:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.minttyrc:
    ~/.profile:
    ~/.p10k.zsh:
    ~/.ripgreprc:
    ~/.screenrc:
    ~/.vimrc:
    ~/.zshenv:
    ~/.zshrc.host:
    ~/.zshrc:
