- defaults:
    link:
      relink: true
      force: false

- clean: ['~']

- link:
    ~/.Xdefaults:
    ~/.alias.host:
    ~/.alias:
    ~/.antigen.zsh:
    ~/.bash_logout:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.minttyrc:
    ~/.p10k.zsh:
    ~/.profile.env:
    ~/.profile.host:
    ~/.profile:
    ~/.ripgreprc:
    ~/.screenrc:
    ~/.vim:
    ~/.vimrc:
    ~/.zshenv:
    ~/.zshrc.host:
    ~/.zshrc:
