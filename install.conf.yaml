- defaults:
    link:
      relink: true
      force: false

- clean: ['~']

- link:
    ~/.Xdefaults:
    ~/.asdfrc:
    ~/.alias:
    ~/.antigen.zsh:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.env:
    ~/.gitconfig:
    ~/.minttyrc:
    ~/.p10k.zsh:
    ~/.ripgreprc:
    ~/.screenrc:
    ~/.secrets:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
