- clean: ['~']

- link:
    ~/.Xdefaults:
    ~/.alias:
    ~/.antigen.zsh:
    ~/.bash_logout:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.minttyrc:
    ~/.profile:
    ~/.ripgreprc:
    ~/.screenrc:
    ~/.vimrc:
    ~/.zshenv:
    ~/.zshrc.host:
    ~/.zshrc:
